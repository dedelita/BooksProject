{% extends "base.html.twig" %}
{% block body %}

{% if not(app.user.isVerified) %}
  Confirm your email. If you didn't receive the email: <a href="{{ path('send_conf_email') }}">resend mail</a>
{% endif %}

<div>
<div><i class="bi bi-plus-circle"></i> {{ 'book.new' | trans }}</div>
</div>
  <div class="container books">
    <div class="heading">
      <h2 class="mt-3 mb-0 pb-0"><i class="bi bi-book"></i> {{ 'my.books' | trans }}</h2>
      <a href="{{ path('get_books') }}">{{ 'see_more' | trans }}</a>
    </div>
    {% include "books/list_grid.html.twig" with {'books': books|slice(0, 5)} %}
  </div>

{#<div class="row">
   <div class="col-sm-6">
<div class="card">
  <h6 class="card-header">{{"my.genres" | trans }}</h6>
  <div class="card-body">
    {% for genre in genres %}
    {% if genre == "" %}
      {% set genre = "unknown" | trans %}
    {% endif %}
        <span class="card-text row px-4 t5"><a href="{{ path('byGenre', {'genre': genre}) }}">{{genre}}</a></span>
    {% endfor %}
    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
    <a href="#" class="btn btn-link btn-sm">Go somewhere</a>
    </div>
  </div>
</div>
 </div> 
  <div class="col-sm-6">
<div class="card border-dark">
  <h6 class="card-header border-dark">{{"my.authors" | trans }}</h6>
  <div class="card-body">
    {% for author in authors %}
        <span class="card-text row px-4 t5"><a href="{{ path('byAuthor', {'author': author}) }}">{{author}}</a></span>
    {% endfor %}
    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
    <a href="#" class="btn btn-link btn-sm">Go somewhere</a>
    </div>
  </div>
</div>
 </div>
</div>

</div>#}
{% endblock %}